## Setup

Install a compatible PyTorch version, then install the Python dependencies:

```bash
pip install -r requirements.txt
```

## Inference

put all wav in "samples/raw"

Baseline inference:

```bash
bash infer.sh
```
